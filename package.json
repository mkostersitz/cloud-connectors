{
  "name": "cloud-connectors",
  "version": "0.1.0",
  "private": true,
  "description": "Monorepo: MCP connectors for personal cloud services (Microsoft personal account, iCloud) sharing a common core",
  "workspaces": [
    "packages/*"
  ],
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "npm run build --workspace=@cloud-connectors/core && npm run build --workspace=windows-live-connector && npm run build --workspace=icloud-connector",
    "typecheck": "npm run typecheck --workspaces --if-present"
  },
  "license": "MIT"
}
